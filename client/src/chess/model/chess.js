class Game{
    constructor(playerWhite){
        this.playerWhite=playerWhite;// bolean   
        this.chessBoard=this.makeStartingBoard();
    }
    makeStartingBoard(){
        //orientation for the current player
        if (this.playerWhite) {
            // for the white player
        } else {
            // this is for the black player
        }

        movePiece(){
            // piece id 
            // to [x,y]
        }
    }
}